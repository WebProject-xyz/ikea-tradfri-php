on:
  push:
    branches:
      - "main"
  pull_request:
  schedule:
    - cron: '24 10 * * 4'

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@04fb7fb89355cc327d31693702ee87f21e137a06" # main
    secrets: inherit
    with:
      php-version: "8.5"
      coverage-path: "build/logs/"
      with-codacy: true
      with-codecov: true
      test-matrix: |
        [
          {"php-version": "8.5", "dependencies": "locked",  "with_coverage": true},
          {"php-version": "8.5", "dependencies": "highest", "with_coverage": false}
        ]
